int x = 2;

for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= i; j++) {
        x = x + j;

        if (x % 2 == 0) {
            x = x + i; // step change inside condition
        }

        System.out.print(x + " ");
    }
}
